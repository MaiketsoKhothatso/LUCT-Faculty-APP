query GetMyHabits @auth(level: USER) {
      habits(where: {userId: {eq_expr: "auth.uid"}}) {
        id
        name
        description
        isPublic
        createdAt
        colorHex
        targetFrequency
      }
    }